header:
  version: 14
  includes:
    - features/ota/ostree/scarthgap.yml

repos:
    meta-modular-bsp-ota:
    url: "https://github.com/Advantech-EECC/meta-modular-bsp-ota.git"
    branch: "styhead-next"
    path: "layers/meta-eecc-ota"
    layers:
      meta-ostree-mbsp-nxp:

local_conf_header:
  ostree: |
    WKS_FILE = "sdimage-nxp.wks.in"

    require conf/distro/sota.conf.inc

    INHERIT += " sota"
